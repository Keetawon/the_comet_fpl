# Claude Code shared project instructions

@AGENTS.md

`AGENTS.md` is the single source of truth for repository status, correctness policy, priorities,
and sub-agent instructions. Update it in place; do not copy those instructions into this file.

Repository skills live only under `.claude/skills/`; these files are the single source of truth
for Claude Code and every other agent.
