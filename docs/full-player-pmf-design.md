# Full-player points synthesis V1: preregistration

Candidate `retrospective_player_points_synthesis_v1`. Evidence class:
`retrospective_competitive_backfill_and_archive_price_proxy_development`.
This is a new full-points composition experiment, not another fit/evaluation of
any frozen upstream candidate. No production/default or optimizer change.

## Comparator and acceptance

The comparator is the **retrospective proxy implementation of the current
prospective selector**, combined with its unchanged joint full-points composer.
It is NOT exact historical deadline reproduction. Source fingerprints bind the
retained typed current reference, actual current selector implementation and rules.
The original821 missing historical-registry rows remain explicit. Of those,
270 belong to this nominated population; archive fixture `value` is used ONLY
by the previously authorized price-sensitive cold-start path in BOTH arms.
All other reference components and established-row inputs remain unchanged.

Apply the owner's predeclared rule mechanically: full-gate-passed successor,
otherwise incumbent. A remains inconclusive; D/E do not satisfy unchanged
181-fold contracts; F/G fail their guardrails. Only H GK saves is accepted.
C's hypothetical-start role probabilities are not a standalone points component;
role/workload are diagnostic selectors here, never a hidden replacement of minutes.
Do not substitute the better matched role-persistence diagnostic after inspection.

Final development stack: incumbent team Goal/CS, current-proxy minutes,
current coupled goals/assists, incumbent DC, **H saves**, absent cards, unchanged
joint BPS/bonus. Rare penalties/own-goal prediction gaps remain as before. The
composer draws minutes once; no nonappearance goal/assist/save/DC/card credit.
No new appearance gating, seed stream, exposure scaling, goals support, BPS
residual, team identity, probability cap, or score constants are introduced.

The exact current BPS calculation directly conditions on minutes, goals, assists
and conceded; saves/DC/cards are carried only indirectly in the unchanged BPS
residual, not explicitly fed from their newly drawn counts. J preserves this
known limitation. It does not launch the out-of-scope Bonus/BPS successor or claim
that a changed save draw directly changes its simulated BPS.

H's saved fold precision, unchanged save fraction and frozen Phase A prequential
opponent-shot predictions are applied to EVERY roster GK, including target DNPs.
Never use inclusion in H's appeared-only score population to select a keeper.
Saved parameter projection is outcome-free, does not fit again, and must exactly
reproduce every overlapping H PMF. The unchanged composer determines appearances
from its random minutes draw. H retains nullable source SOT and its selective-
omission caveat; no missing SOT is converted into zero.

## Population and target fixed by coverage before G/H results

Use `docs/full-player-pmf-population-audit.md` unchanged:2025-26,38GW,
380fixtures,29,747roster rows including DNPs,270direct price proxies.100% measured
target components required.2023-24/2024-25 have no DC labels and are not zero-filled.
One season cannot establish cross-season robustness.

Replay all measured components INCLUDING bonus/cards/penalties/own goals with
loaded2026/27 rules. Keep signed observed points. The legacy composer has0..34
support, so the proper-score target is explicitly `min(34,max(0,signed_target))`.
The77 negative labels and one zero-minute -1 remain retained. Report signed bias
and MAE separately. Do not describe this as a calibrated signed-points PMF.
The fixed2000 joint draws use existing fixture seed derived from base202627.
No seed/draw search. Retain current and candidate full PMFs and bonus diagnostics.
The inherited1e-12 log floor and zero empirical target-bin counts are disclosed;
no post-result PMF smoothing. Floored log is an approximation with limited strict
properness, not proof of a fully specified continuous predictive law.

## Event time, cross-fitting and numerical policy

Reuse complete GW reference batches with their frozen first-kickoff cutoff proxy.
Do not absorb early DGW legs into a later fixture within that same GW. Reference
input source guards and saved upstream OOS certificates remain mandatory; source
known_at stays actual, even where later than historical cutoff. Strict prospective
PointInTimeView is never bypassed. No target ICT/lineup/observed minutes enters a
predictor. Inherited conditional-independence approximations remain: components
are sampled conditionally on the same minutes draw; joint BPS gives fixture bonus
coupling but is not a fully coherent shared team scoreline allocation.

No new fitting, hyperparameter grid, scaling or tuning. Exact fixed arithmetic and
deterministic identity ordering; there is therefore no selection tie to resolve.
Retained source version-selection and original event cutoffs are unchanged.
Role confidence is diagnostic only: at least one prior measured start, then max
frozen candidate probability >=.75 is high, else low; no history is unavailable.
Rotation risk uses incumbent P(60+) <.75. Workload uses only positive witnessed
nominal7d lower-bound evidence: >90, (0,90], or unavailable. It is NOT complete
rest or a proved congestion classification. Promoted status uses stable team_code
and prior-season membership, not future outcomes.

Workload slices specifically use D's retained usable feature evidence: role/cold/
identity fallback can make this field unavailable even where some positive raw
witnesses exist. They are not a census of every witnessed appearance. This
diagnostic limitation never changes the nominated roster or prediction inputs.

## Single formal run and gate

Commit implementation/config/tests/documentation cleanly. Reproduce the incumbent
FIRST for all380fixtures using both the unmodified reference composition function
and the new wrapper with no replacements. Every PMF AND bonus diagnostic must
match exactly (zero tolerance). Retain these control receipts without overwriting
any incumbent artifact. Only then reserve the exclusive shared Git candidate
claim and simulate the synthesis once. Dirty worktree, source drift, output reuse,
identity mismatch or failed reproduction fails closed; no allow-dirty/resume.

Primary gate: >=1% relative mean floored negative-log-score improvement on the
same nominated rows. Also no CRPS regression, PIT80 absolute error <=.05,
no major-position log-score regression >1%, and within-GW ranking relative
regression <=1%. Zero temporal violations and complete identity population are
mandatory. One-season robustness remains unproved, not invented as a pass.
SUPPORTED if every registered check passes; REFUTED if primary regression >=1%;
otherwise INCONCLUSIVE. All verdicts remain development-only and no promotion.

Report overall, season, GW1-6/GW7+, home/away, all four positions, promoted/
established teams, cold/established players, role confidence, rotation risk and
observed-workload scope. Report270direct proxies, nonproxies, and conservative
team-/fixture-propagated dependence. Exclusion is diagnostic only and cannot
replace the primary cohort after results. No price premium split without an
existing fixed contract. Report NLL/CRPS/PIT, MAE/bias, expected points, blank<=2,
5+/10+ Brier/reliability, within-GW ranking, full paired player/fixture losses,
GW-clustered SE and normal interval (not serial-dependence adjusted).

Pin config/source/tests/docs/frozen-result SHA256, clean Git HEAD, research DB,
all114 minutes/current-reference folds,38 role folds/source ledger, complete D
workload evidence, H114 folds and Phase A OOS evidence. Recheck source and output
hashes before publishing the result. Original database/default path unchanged.

## Bounded runtime

No model fits.380 old-reference plus380 zero-wrapper reproductions, then380
candidate compositions:1140 joint calls /2.28million fixture-world draws.
Synthetic80-player/2000-draw composition was0.289s and fixed lookup construction
0.116s. Approximate CPU budget6.2minutes including repeated reference lookup
construction, plus provenance/I/O/scoring. Reserve20minutes on this machine with
no tuning permitted. Unexpected combinatorial growth must fail before scoring.

## Pre-run verification

The data-only integration preflight passes728,434checks with520unchanged source
hashes, all29,747target identities and3,427GK projections (767exact H overlaps).
It invokes no model fit, performance score, Monte Carlo, formal runner or claim.
The broader partition records3,571passed/1failed/4skipped before a test-only fix:
an inherited order-dependent test sometimes changed a last-row GK into GK and
therefore supplied no invalid input. It now always changes that row's position;
the model/reference implementation is untouched. The corrected relevant gate
passes180tests. Ruff and strict mypy189source files pass. Eleven unrelated
pre-existing formatting files remain, alongside separately documented Windows
symlink privilege failures; the full repository gate is not labelled all-green.
